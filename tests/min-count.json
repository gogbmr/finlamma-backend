{
  "minTestCount": 890,
  "note": "The floor scripts/check-test-count.mjs (posttest) enforces - pnpm test fails if the real count drops below this. Deliberately set a bit below the real current count (890 vs the ~902 actually passing as of 2026-09-23), not pinned exactly to it: the goal is to catch an accidental bulk loss (like a Write silently overwriting a test file - see CLAUDE.md's 'never write a test file without reading it first' rule), not to force a manual bump on every small, deliberate test removal or consolidation. Still bump this value deliberately, in the same commit, whenever a large/intentional drop in test count would otherwise silently eat into the buffer."
}
